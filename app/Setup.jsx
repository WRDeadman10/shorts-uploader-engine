import React, { useState } from 'react';
import { useAppStore } from './useAppStore.js';

export default function Setup() {
    const { envCheckResult, runEnvCheck } = useAppStore();
    const [checking, setChecking] = useState(false);

    async function handleCheck() {
        setChecking(true);
        await runEnvCheck();
        setChecking(false);
    }

    return (<div className='page-content'><h1 className='page-heading'>Environment Setup</h1><div style={{marginBottom:16}}><button onClick={handleCheck} disabled={checking} style={{padding:'9px 22px',borderRadius:7,border:'none',background:'#4f46e5',color:'#fff',cursor:'pointer',fontSize:14'}}>{checking ? 'Checking...' : 'Run Diagnostics'}</button></div>{envCheckResult === null && (<p style={{color:'#6b7280',fontSize:13}}>Click Run Diagnostics to verify your environment.</p>)}{envCheckResult !== null && (<div style={{display:'flex',flexDirection:'column',gap:10}}><div style={{background:'#111827',borderRadius:8,padding:'14px 18px'}}><p style={{color:'#9ca3af',fontSize:11,marginBottom:8,textTransform:'uppercase',letterSpacing:'0.05em'}}>Runtime</p><div style={{display:'flex',flexDirection:'column',gap:6}}><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:envCheckResult.python.found?'#4ade80':'#ef4444',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>Python {envCheckResult.python.found ? (envCheckResult.python.version||'found') : 'not found'}</span></div><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:envCheckResult.ffmpeg.found?'#4ade80':'#ef4444',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>ffmpeg {envCheckResult.ffmpeg.found ? (envCheckResult.ffmpeg.version||'found') : 'not found'}</span></div><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:envCheckResult.ffprobe.found?'#4ade80':'#ef4444',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>ffprobe {envCheckResult.ffprobe.found ? (envCheckResult.ffprobe.version||'found') : 'not found'}</span></div></div></div><div style={{background:'#111827',borderRadius:8,padding:'14px 18px'}}><p style={{color:'#9ca3af',fontSize:11,marginBottom:8,textTransform:'uppercase',letterSpacing:'0.05em'}}>Credentials</p><div style={{display:'flex',flexDirection:'column',gap:6}}><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:(envCheckResult.credentials&&envCheckResult.credentials.clientSecret)?'#4ade80':'#ef4444',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>client_secret.json {(envCheckResult.credentials&&envCheckResult.credentials.clientSecret)?'found':'missing'}</span></div><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:(envCheckResult.credentials&&envCheckResult.credentials.token)?'#4ade80':'#f59e0b',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>token.json {(envCheckResult.credentials&&envCheckResult.credentials.token)?'found':'not yet generated'}</span></div></div></div><div style={{background:'#111827',borderRadius:8,padding:'14px 18px'}}><p style={{color:'#9ca3af',fontSize:11,marginBottom:8,textTransform:'uppercase',letterSpacing:'0.05em'}}>Paths</p><div style={{display:'flex',flexDirection:'column',gap:6}}><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:(envCheckResult.settings&&envCheckResult.settings.found)?'#4ade80':'#f59e0b',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>settings.json {(envCheckResult.settings&&envCheckResult.settings.found)?'found':'not yet created'}</span></div><div style={{display:'flex',alignItems:'center',gap:10}}><span style={{width:10,height:10,borderRadius:'50%',background:(envCheckResult.videoRoot&&envCheckResult.videoRoot.found)?'#4ade80':'#ef4444',display:'inline-block'}}></span><span style={{color:'#e2e8f0',fontSize:13}}>Video root {(envCheckResult.videoRoot&&envCheckResult.videoRoot.found)?'found':'not found'}{envCheckResult.videoRoot&&envCheckResult.videoRoot.path?(' — '+envCheckResult.videoRoot.path):''}</span></div></div></div></div>)}</div>);
}
