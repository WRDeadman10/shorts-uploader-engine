# Dashboard Missing Operational Actions

The Dashboard is currently read-only.

Current behavior:

- it shows counts and progress bars
- it does not provide direct operational actions

Useful missing actions:

- refresh report / library state
- jump to missing-platform queues
- run local status report
- run live audit
- resume or inspect the latest upload process

Expected outcome:

- dashboard as an operator command center, not just a stat board
