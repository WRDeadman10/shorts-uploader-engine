# Music Workflow Settings

The Electron UI has only a simple "Music Overlay" toggle and does not expose the real music workflow controls.

Missing settings include:

- music directory
- music inventory file
- background volume
- optional sample video / sample music / output paths

Why it matters:

- background music selection is not just on/off
- the repo includes a separate `musicOverlaySample.py` workflow

Expected outcome:

- upload flow settings for music mixing
- dedicated music sample flow in the Electron UI
