# Library Missing Search Sort And Details

The Library page shows cards, but important operator features are missing.

Current gaps:

- no text search
- no sort controls
- no detailed side panel for the selected asset
- no quick access to source path, metadata path, uploaded file path, or music file

Why it matters:

- the data is already available from `dataService.js`
- operators need faster inspection and triage at scale

Expected outcome:

- richer library tooling around the existing video rows
